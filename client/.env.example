# Base path for API calls. "/api" works both in production (same Vercel
# deployment) and in local dev when running `vercel dev` (serves client + api
# together). Only change this if the API is hosted separately.
VITE_API_BASE_URL=/api
